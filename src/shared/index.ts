/**
 * Shared exports for Convex Better Auth package
 * Use these in both client-side (TanStack Start) and server-side (Convex) code
 */

// Configuration types and utilities
export * from './config'

// Shared type definitions
export * from './types'
